{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edition du mot de passe de {{ user.username }}</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form.plainPassword) }}
    <input type="submit" value="Editer" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('user_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
